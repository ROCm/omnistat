name: debug

on:
  workflow_dispatch:

  push:
    branches: ["noop"]

jobs:
  gpu-collectors-325:
    runs-on: [linux-mi325-1gpu-ossci-rocm]

    steps:
      - name: Interrogate
        run: |
          rocm-smi
          which python3
          amd-smi version
          cat /etc/os-release
          ps auxw
      - name: Interrogate2
        run: |
          unset ROCR_VISIBLE_DEVICES
          rocm-smi

      